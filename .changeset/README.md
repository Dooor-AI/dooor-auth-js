# Changesets

This directory is managed by [Changesets](https://github.com/changesets/changesets).

Run `pnpm changeset` after a code change that should be released, then `pnpm version-packages` to bump versions and update changelogs before publishing.
